import os

DATA_FILE = "items.toml"
ITEM_FILE = os.path.join(["data", "items.toml"])
ENTITY_FILE = os.path.join(["data", "entities.toml"])
ENEMY_FILE = os.path.join(["data", "enemies.toml"])
NPC_FILE = os.path.join(["data", "npcs.toml"])
